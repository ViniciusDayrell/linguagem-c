#include <stdio.h>
#include <stdlib.h>

int main(){
    int n;
    printf ("Digite um numero: \n");
    scanf ("%d", &n);
      if ((n%2==0 && n > 100) || (n%2!=0 && n < 70)){
            printf ("Parabens!");
      } else {
          printf ("Sinto muito!");
      }
    return 0;
}
